import { useState } from "react";

const Complaints = () => {
  const [complaints] = useState([]);

  return <></>;
};

export default Complaints;

/** - Complaints / state:nts[{row_id:Number, content:String, status:Bool}] =
 * useState([]) / =>@Lady
 */
// const Compliants = () => {}
